import {useParams} from 'react-router-dom'

const SingleVideoPage = () => {
  const {id} = useParams()
  const [video, set] 
  return (
    <div>
      
    </div>
  )
}

export default SingleVideoPage
